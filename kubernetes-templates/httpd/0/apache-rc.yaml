apiVersion: v1
kind: ReplicationController
metadata:
  name: Apache
spec:
  replicas: ${REPLICAS}
  selector:
    os: httpd
  template:
    metadata:
      labels:
        os: httpd
    spec:
      restartPolicy: Always
      containers:
      - name: httpd
        image: httpd
        imagePullPolicy: Always
